/**
 *  list of single words
 */
export const words = ['hello', 'I\'m','you', 'tonight', 'words', 'that', 'doesn\'t', 'exists', 'in', 'example'];

/**
 * list of expressions (multiple words only)
 */
 export const expressions = ['i\'m fine','something they did not say'];
